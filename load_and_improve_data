@st.cache_data
def load_and_improve_data():
    import os
    # This will show you exactly what files are in the folder
    files_in_repo = os.listdir(".")
    st.write("Files Streamlit can see in your repo:", files_in_repo)
    
    # Check if our target file is in that list
    target_file = 'audit_data.xlsx'
    
    if target_file not in files_in_repo:
        st.error(f"❌ '{target_file}' NOT FOUND. Look at the list above—is it spelled differently?")
        st.stop()
        
    df = pd.read_excel(target_file, engine='openpyxl')
    # ... rest of your code ...
    return df
